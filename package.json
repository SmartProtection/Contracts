{
  "scripts": {
    "prettier": "prettier --write contracts/**/*.sol",
    "compile": "npx hardhat compile",
    "docgen": "npm run compile && npx solidity-docgen --solc-module solc",
    "deploy:sepolia": "npm run compile && npx hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@metamask/eth-sig-util": "^5.0.2",
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3",
    "fs": "^0.0.1-security",
    "prettier": "^2.8.4"
  },
  "devDependencies": {
    "hardhat": "^2.13.0",
    "prettier-plugin-solidity": "^1.1.3",
    "solc": "^0.8.18",
    "solidity-docgen": "^0.5.17"
  }
}
